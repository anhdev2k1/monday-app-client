const routes = {
   // client
   home: '/',
   login: '/login',
   register: '/register',
   // private router
   board: '/board/:id',
   workspace: '/workspace/:idWorkspace',
};

export default routes;
