const routes = {
   // client
   home: '/',
   login: '/login',
   register: '/register',
   registerStep2: '/register-step2',
   // private router
   board: '/board/:id',
   workspace: '/workspace/:id',
};

export default routes;
