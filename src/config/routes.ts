const routes = {
   // client
   home: '/',
   login: '/login',
   register: '/register',
   // private router
   board: '/board/:idBoard',
   workspace: '/workspace/:idWorkSpace',
};

export default routes;
